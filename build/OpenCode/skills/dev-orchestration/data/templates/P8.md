# Phase 8: 交付验证

## 步骤概览

| 步骤 | Agent | 组 | 并行 |
|------|-------|---|------|
| self-verify | 编排者自身 | g1 | 串行 |
| oracle-final | @oracle | g2 | 串行 |
| delivery-summary | 编排者自身 | g3 | 串行 |

已完成步骤: {{COMPLETED_STEPS}}

## 委托质量标准

> 委托质量标准请务必遵循主 SKILL 协议。

P8 追加：对抗验证（oracle）注入攻击面提示 + 修改文件列表 + 关键假设清单。编排者自身步骤走查全量验证矩阵。

## Step: self-verify

> Agent: 编排者自身 | Phase 8 — 交付验证
> P8 g1

**测试方法论参考** 如需补充测试，参考 `c:\Users\Dragon\.copilot\skills\ai-testing-workflow\SKILL.md` 获取完整测试工作流。

0. **一致性扫描**：grep 关键术语（确认前后一致）+ grep 关键数值（无矛盾）+ grep 旧术语（无残留）；代码类再加全量构建+测试+类型+lint 全绿
1. **构建+测试+Lint**：tsc/mypy → eslint/ruff → 单元+集成测试 — 全部 exit 0；任一失败 → 修复 → 重跑
2. **傻瓜视角走查**：假设从未见过项目 — 按 README 从零操作，核心流程手动走一遍，非法输入触发边界，检查 env/config 模板完整性
3. **交叉验证**：
   - 产出 vs `.sisyphus/plans/{{TASK_NAME}}.md`：逐条核对无遗漏
   - 代码 vs P3 方案：一致或偏差已记录
   - API vs {{CONTRACTS_PATH}}：字段/路径/状态码逐项比对（如有）
   - 命名一致性：同一概念全栈同名
4. **Oracle Gate 咨询（必做）**：`dev-orch dispatch --cross oracle-consult`
   - 咨询需覆盖 upstream/downstream 依赖影响，结果写入 `{{REVIEW_DIR}}/P8-<gate>.md`。

**结果写入** `{{LOG_DIR}}/p8-self-verify.md`

> **“完成”定义**：Build passes ≠ Done。必须同时满足：功能实际工作（API→数据正确→持久化）· UI 实际渲染（非 Loading/placeholder）· 用户流端到端（操作→变更→持久化→刷新仍在）· 错误状态已处理。

**退出**
```
dev-orch done P8 self-verify
dev-orch mark P8 self_verify_pass passed "{{REVIEW_DIR}}/P8-self_verify_pass.md"
dev-orch mark P8 consistency_scan_pass passed "{{REVIEW_DIR}}/P8-consistency_scan_pass.md"
```

## Step: oracle-final

> Agent: @oracle [ADVERSARIAL] | Phase 8 — 交付验证
> P8 g2

**注入上下文**：实现摘要 + 关键假设清单 + 修改文件列表 + 攻击面提示（编排者认为最薄弱的环节）。

**MUST DO**：
1. **架构一致性**：实现 vs {{CONVENTIONS_PATH}} — 目录结构、模块边界、数据流向；未记录偏差 = 问题
2. **安全审计**：密钥硬编码 / 输入校验 / 认证授权 / 参数化查询 / XSS / CORS / 依赖 CVE
3. **性能+可维护性**：N+1 / 缺失索引 / 重渲染 / Bundle / 内存泄漏 / 分页 / 循环依赖
4. **契约遵守**：{{CONTRACTS_PATH}} vs 实际 API 逐项比对（如有）
5. **输出格式**：
   - `VERDICT: PASS | FAIL`
   - 假设验证清单（每条假设 → 验证/推翻）
   - 攻击报告（每维度 → 发现 / 无发现）
   - 修复建议优先级排序（BLOCKER → RISK → MINOR）

**结果写入** `{{LOG_DIR}}/p8-oracle-final.md`

退出：`dev-orch done P8 oracle-final`

## Step: delivery-summary

> Agent: 编排者自身 | Phase 8 — 交付验证
> P8 g3

**最终验收 checklist**（全通过方可继续）：
- [ ] 完整系统可启动
- [ ] 核心用户流走通（CRUD+鉴权+边界）
- [ ] 错误状态返回正确
- [ ] 数据持久化正常
- [ ] 全量测试通过
- [ ] 构建+类型+lint 通过

撰写交付摘要并默认结束交付验证；仅当当前任务明确要求业务验收结论，或仍缺少只有用户能提供的权限/环境/业务决定时，才 ask_user 呈递：

| 产出 | 路径 | 状态 |
|------|------|------|
| 计划文件 | {{PLAN_OUTPUT_PATH}} | ✅ |
| CONVENTIONS | {{CONVENTIONS_PATH}} | ✅/N/A |
| DOCTRINE | {{DOCTRINE_PATH}} | ✅/N/A |
| CONTRACTS | {{CONTRACTS_PATH}} | ✅/N/A |
| 后端代码 | [路径] | ✅/N/A |
| 前端代码 | [路径] | ✅/N/A |

**最终验证矩阵**（按 `{{MODE}}` 决定哪些列适用）：

| 验证项 | fullstack | backend | frontend |
|--------|:---------:|:-------:|:--------:|
| 全量构建 | ✅ | ✅ | ✅ |
| 全量测试 | ✅ | ✅ | ✅ |
| 类型检查 | ✅ | ✅ | ✅ |
| 契约验证 | ✅ | ✅ | ⚠️ 有 API 则验 |
| E2E 端到端 | ✅ | ⚠️ API 级 | ⚠️ 有后端则验 |
| lint 零新增 | ✅ | ✅ | ✅ |
| Mock 清理 | ✅ | N/A | ✅ |

交付摘要模板：
```markdown
# 交付摘要
## 范围
- 模式：[{{MODE}}]
- Phase 执行路径：[P0 → ... → P8]
## 产出物
| 产出 | 路径 | 状态 |
|------|------|------|
## 验证结果
| 检查 | 结果 |
|------|------|
| 构建 | ✅/❌ |
| 测试 | ✅ X/Y passed |
| 类型 | ✅/❌ |
| 契约 | ✅/❌/N/A |
| Oracle | ✅ PASS |
## 已知限制
- [如有]
```

**结果写入** `{{LOG_DIR}}/delivery-summary.md`

**退出**（默认自动继续；仅命中三类 ask_user 例外时等待用户结论）
```
dev-orch done P8 delivery
如命中 ask_user 例外并收到验收结论：
dev-orch mark P8 user_accepted passed "{{REVIEW_DIR}}/P8-user_accepted.md"
```

## Gate 二审（必做）

- 先执行 `dev-orch dispatch --cross oracle-consult` 做最终依赖影响复核。
- 生成 `{{REVIEW_DIR}}/P8-<gate>.md`，必须包含 Upstream/Downstream 依赖影响。
- 完成证据文件后再执行 `dev-orch mark P8 <gate> passed "<review-path>"`。
